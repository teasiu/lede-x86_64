# 基于lean lede 的 x86_64固件

## 说明

- ipv4: 192.168.8.1
- username: root
- password: password

## tips: create patch files
```
git add .
git commit -m "set some change by default"
git format-patch -s -1,2...
```
